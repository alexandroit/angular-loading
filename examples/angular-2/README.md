# Angular 2 Wrapper Notes

This repository starts with the Angular 2 line for `@revivejs/angular-loading`.

The live docs shell is generated into:

```text
docs/angular-2/
```

and mirrors the source app in:

```text
docs-src/angular-2/
```
